export const UserMessages = ({reciverId, setSenderId}) => {
    return (
        <header className="w-full flex justify-center">
            This is the messages component.It should populate all messages that this user's id is linked to. < br/>
            And then display : Sender profile pic, Name of sender, message from sender
        </header>
    )
}
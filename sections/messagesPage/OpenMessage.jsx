export const OpenMessage = ({}) => {
    return (
        <header className="w-full flex justify-center">
            <div>
                This is the open message component. It should populate all messages between this user, and the sender that they clicked on. < br/>
                And then display : top(sender profile picture, sender name) middle(previous messages) bottom(type text field and send button)
            </div>
        </header>
    )
}